<% choices = ['1','2','3','4','5','omit'] %>
<% choices.each do |choice| %>
	<span style="margin-right:3px;"><%= radio_button_tag "response_#{questioning_id}", choice, :class => 'radio inline' %></span>
<% end %>